apiVersion: apps/v1
kind: Deployment
metadata:
  name: server-a
  labels:
    app: server-a
spec:
  replicas: 1
  selector:
    matchLabels:
      app: server-a
  template:
    metadata:
      labels:
        app: server-a
    spec:
      containers:
        - name: server-a
          image: master-1:30002/project/server-a:20260215-202210
          ports:
            - containerPort: 8081
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: GATEWAY_URL
              value: "http://spring-cloud-gateway:8000"
            - name: LOG_LEVEL_ROOT
              value: "INFO"
            - name: LOG_LEVEL_APP
              value: "INFO"
---
apiVersion: v1
kind: Service
metadata:
  name: server-a
spec:
  type: ClusterIP
  selector:
    app: server-a
  ports:
    - port: 8081
      targetPort: 8081
